{
    "$schema": "https://railway.app/railway.schema.json",
    "deploy": {
        "startCommand": "python manage.py migrate && python manage.py collectstatic --noinput && gunicorn selleryltd.wsgi:application --bind 0.0.0.0:${PORT}"
    },
    "build": {
        "builder": "NIXPACKS",
        "nixpacksPlan": {
            "requires": ["python"]
        }
    }
}